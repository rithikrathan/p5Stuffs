/* autogenerated by Processing revision 1313 on 2026-04-18 */
import processing.core.*;
import processing.data.*;
import processing.event.*;
import processing.opengl.*;

import java.util.HashMap;
import java.util.ArrayList;
import java.io.File;
import java.io.BufferedReader;
import java.io.PrintWriter;
import java.io.InputStream;
import java.io.OutputStream;
import java.io.IOException;

public class HeartCursion extends PApplet {

//objects
ArrayList<point> Points = new ArrayList<point>();
float[][] scaledPoints;

int innerColor = color(70, 10, 10);
int outerColor = color(100, 10, 10);
int[] curveColors;
int[] trailAlphas;
float[] trailWeights;

float digScale  = 2.8f;
float resolution = 0.01f;
int  shapeCount = 15;
float shapeSpace = 0.7f;
boolean export = false;

float time = 0;
float speed = 69;
int trailLength = 200;

public void setup() {
    frameRate(60);
    /* size commented out by preprocessor */;
    background(100,100,190);
    scale(1,-1);
    heartCurve(0, 0, digScale);
    
    scaledPoints = new float[Points.size()][2];
    for (int i = 0; i < Points.size(); i++) {
        scaledPoints[i][0] = Points.get(i).x;
        scaledPoints[i][1] = Points.get(i).y;
    }
    
    curveColors = new int[shapeCount];
    for (int i = 0; i < shapeCount; i++) {
        float t = PApplet.parseFloat(i) / (shapeCount - 1);
        curveColors[i] = lerpColor(innerColor, outerColor, t);
    }
    
    trailAlphas = new int[trailLength];
    trailWeights = new float[trailLength];
    for (int t = 0; t < trailLength; t++) {
        trailAlphas[t] = PApplet.parseInt(map(t, 0, trailLength, 255, 0));
        trailWeights[t] = map(t, 0, trailLength, 4, 1);
    }
}

public void draw() {
    watermarkBackground(color(3,3,10), color(20), "@rathan_rithik");
    translate(600,600);
    scale(1,-1);

    drawAxes(580.0f);

    for (int i = 0; i < shapeCount; i++) {
        float scaleFactor = (i + 1) * shapeSpace;
        
        stroke(80);
        strokeWeight(0.5f);
        noFill();
        beginShape();
        for (int j = 0; j < scaledPoints.length; j++) {
            vertex(scaledPoints[j][0] * scaleFactor, scaledPoints[j][1] * scaleFactor);
        }
        endShape();
        
        stroke(curveColors[i]);
        strokeWeight(3);
        beginShape();
        for (int j = 0; j < scaledPoints.length; j++) {
            vertex(scaledPoints[j][0] * scaleFactor, scaledPoints[j][1] * scaleFactor);
        }
        endShape();

        for (int t = 0; t < trailLength; t++) {
            float trailTime = time / scaleFactor - t * 0.8f;
            if (trailTime < 0) trailTime += scaledPoints.length;
            int idx = PApplet.parseInt(trailTime) % scaledPoints.length;
            
            int alpha = trailAlphas[t];
            stroke(255, 40, 40, alpha);
            strokeWeight(trailWeights[t]);
            
            point(scaledPoints[idx][0] * scaleFactor, scaledPoints[idx][1] * scaleFactor);
        }
    }

    time += speed;

    if (export) {
        saveFrame("videoFrames/frame-########.png");
    }
}
public void drawAxes(float length) {
    stroke(100);     
    strokeWeight(1); 
    fill(100);
    float axisLen = length; 
    line(-axisLen, 0, axisLen, 0); 
    line(0, -axisLen, 0, axisLen); 
    float arrowSize = 6;
    pushMatrix();
    translate(axisLen, 0);
    triangle(0, 0, -arrowSize, -arrowSize/2, -arrowSize, arrowSize/2);
    popMatrix();
    pushMatrix();
    translate(-axisLen, 0);
    triangle(0, 0, arrowSize, -arrowSize/2, arrowSize, arrowSize/2);
    popMatrix();
    pushMatrix();
    translate(0, axisLen);
    triangle(0, 0, -arrowSize/2, -arrowSize, arrowSize/2, -arrowSize);
    popMatrix();
    pushMatrix();
    translate(0, -axisLen);
    triangle(0, 0, -arrowSize/2, arrowSize, arrowSize/2, arrowSize);
    popMatrix();
    pushMatrix();
    scale(1, -1); 
    textSize(14);
    fill(150);
    textAlign(CENTER, CENTER);
    text("+X", axisLen - 20, 20);      // Right
    text("-X", -axisLen + 20, 20);     // Left
    text("+Y", 20, -axisLen + 20);     // Top (Note negative Y coord)
    text("-Y", 20, axisLen - 20);      // Bottom (Note positive Y coord)
    popMatrix();
}

public void drawDottedLine(float x1, float y1, float x2, float y2, float steps) {
    for (int i = 0; i <= steps; i++) {
        // lerp() calculates a point between 0.0 (start) and 1.0 (end)
        float x = lerp(x1, x2, i / steps);
        float y = lerp(y1, y2, i / steps);
        point(x, y); // Use point() for dots, or small rects/ellipses
    }
}

public void dashedLine(float x1, float y1, float x2, float y2, float dashLen, float gapLen) {
    float totalDist = dist(x1, y1, x2, y2);
    float dx = (x2 - x1) / totalDist; // Unit vector X component
    float dy = (y2 - y1) / totalDist; // Unit vector Y component

    float currentDist = 0;

    while (currentDist < totalDist) {
        // 1. Calculate start of this dash
        float startX = x1 + dx * currentDist;
        float startY = y1 + dy * currentDist;

        // 2. Calculate end of this dash
        float endX = startX + dx * dashLen;
        float endY = startY + dy * dashLen;

        // 3. CLAMP: If this dash goes past the target, stop exactly at the target
        if (currentDist + dashLen > totalDist) {
            endX = x2;
            endY = y2;
        }

        line(startX, startY, endX, endY);

        // 4. Move forward by dash + gap
        currentDist += dashLen + gapLen;
    }
}

public void drawPointLabel(String name, float x, float y, int c, boolean showCoords) {
    float offset = 45; // Distance from point
    float m = dist(0, 0, x, y);
    if (m == 0) m = 1;

    float tx = (x / m) * (m + offset);
    float ty = (y / m) * (m + offset);

    pushMatrix();
    translate(tx, ty);
    scale(1, -1); // Unflip text

    fill(c);
    textAlign(CENTER, CENTER);
    textSize(14);

    String label = name;
    if (showCoords) {
        label += "\n(" + nfc(x, 1) + ", " + nfc(y, 1) + ")";
    }

    text(label, 0, 0);

    popMatrix();
}

public void watermarkBackground(int bgColor, int textColor, String text) {
    background(bgColor);
    
    pushMatrix();
    resetMatrix();
    
    fill(textColor);
    noStroke();
    textSize(24);
    textAlign(CENTER, CENTER);
    
    float angle = -30;
    float diagonal = sqrt(width * width + height * height);
    float spacing = 80;
    
    pushMatrix();
    translate(width / 2, height / 2);
    rotate(radians(angle));
    
    for (float y = -diagonal; y < diagonal; y += spacing) {
        for (float x = -diagonal; x < diagonal; x += textWidth(text) * 1.5f) {
            pushMatrix();
            translate(x, y);
            text(text, 0, 0);
            popMatrix();
        }
    }
    popMatrix();
    
    popMatrix();
}	
class point {
    float x, y;

    point(float x, float y) {
        this.x = x;
        this.y = y;
    }
}

public void heartCurve(float cx, float cy, float scale) {
    for (float t = 0; t < TAU; t += resolution) {
        float xVal = 16 * pow(sin(t), 3);
        float yVal = 13 * cos(t) - 5 * cos(2 * t) - 2 * cos(3 * t) - cos(4 * t);
        float x = cx + scale * xVal;
        float y = cy - scale * yVal; 
        Points.add(new point(x, -y));
    }
}


  public void settings() { size(1200, 1200); }

  static public void main(String[] passedArgs) {
    String[] appletArgs = new String[] { "HeartCursion" };
    if (passedArgs != null) {
      PApplet.main(concat(appletArgs, passedArgs));
    } else {
      PApplet.main(appletArgs);
    }
  }
}
