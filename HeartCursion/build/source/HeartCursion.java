/* autogenerated by Processing revision 1313 on 2026-04-17 */
import processing.core.*;
import processing.data.*;
import processing.event.*;
import processing.opengl.*;

import java.util.HashMap;
import java.util.ArrayList;
import java.io.File;
import java.io.BufferedReader;
import java.io.PrintWriter;
import java.io.InputStream;
import java.io.OutputStream;
import java.io.IOException;

public class HeartCursion extends PApplet {

//objects
ArrayList<point> Points = new ArrayList<point>();

//normal variabls
boolean export = false;
float digScale  = 10;
float increment = 0.02f;
public void setup() {
	frameRate(60);
	/* size commented out by preprocessor */;
	background(100,100,190);
	scale(1,-1);
}

class point {
    float x, y;

    point(float x, float y) {
        this.x = x;
        this.y = y;
    }
}

public void heartCurve(float cx, float cy, float scale) {
    for (float t = 0; t < TAU; t += increment) {
        // We use pow(sin(t), 3) for the X shape
        float xVal = 16 * pow(sin(t), 3);
        float yVal = 13 * cos(t) - 5 * cos(2 * t) - 2 * cos(3 * t) - cos(4 * t);
        float x = cx + scale * xVal;
        float y = cy - scale * yVal; 
        Points.add(new point(x, -y));
    }
}

public void draw() {
	background(3,3,10);
	translate(400,400);
	scale(1,-1);

	// draw logic
	drawAxes();
    heartCurve(0, 0, digScale);

    noFill();
    beginShape();
		for (point vert: Points) {
			vertex(vert.x , vert.y );
		}
    endShape();

	if (export) {
		saveFrame("videoFrames/frame-######.png");
	}
}

public void drawAxes() {
	stroke(100);     
	strokeWeight(1); 
	fill(100);
	float axisLen = 360; 
	line(-axisLen, 0, axisLen, 0); 
	line(0, -axisLen, 0, axisLen); 
	float arrowSize = 6;
	pushMatrix();
	translate(axisLen, 0);
	triangle(0, 0, -arrowSize, -arrowSize/2, -arrowSize, arrowSize/2);
	popMatrix();
	pushMatrix();
	translate(-axisLen, 0);
	triangle(0, 0, arrowSize, -arrowSize/2, arrowSize, arrowSize/2);
	popMatrix();
	pushMatrix();
	translate(0, axisLen);
	triangle(0, 0, -arrowSize/2, -arrowSize, arrowSize/2, -arrowSize);
	popMatrix();
	pushMatrix();
	translate(0, -axisLen);
	triangle(0, 0, -arrowSize/2, arrowSize, arrowSize/2, arrowSize);
	popMatrix();
	pushMatrix();
	scale(1, -1); 
	textSize(14);
	fill(150);
	textAlign(CENTER, CENTER);
	text("+X", axisLen - 20, 20);      // Right
	text("-X", -axisLen + 20, 20);     // Left
	text("+Y", 20, -axisLen + 20);     // Top (Note negative Y coord)
	text("-Y", 20, axisLen - 20);      // Bottom (Note positive Y coord)
	popMatrix();
}


  public void settings() { size(800, 800); }

  static public void main(String[] passedArgs) {
    String[] appletArgs = new String[] { "HeartCursion" };
    if (passedArgs != null) {
      PApplet.main(concat(appletArgs, passedArgs));
    } else {
      PApplet.main(appletArgs);
    }
  }
}
